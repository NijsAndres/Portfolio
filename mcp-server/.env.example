# Copy to .env and fill in. CMS_API_TOKEN must match Laravel's CMS_API_TOKEN.
# Production target (per the CMS guide). Use http://portfolio.test/api/cms locally.
CMS_API_URL=https://andresnijs.be/api/cms
CMS_API_TOKEN=
